# TypeFlow Python Dependencies
# GTK4 / libadwaita bindings come from system packages (not pip):
#   sudo apt install python3-gi gir1.2-gtk-4.0 gir1.2-adw-1
#   sudo apt install xdotool   # X11 keystroke simulation
#   # OR for Wayland:
#   sudo apt install ydotool   # Wayland keystroke simulation

# Global hotkey support (optional but recommended)
pynput>=1.7.6

# Optional: for Wayland clipboard access
# pyperclip>=1.8.2
